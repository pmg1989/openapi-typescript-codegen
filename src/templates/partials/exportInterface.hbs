import { ApiPropertyOptional } from '@nestjs/swagger'
{{#if description}}
/** {{{escapeComment description}}} */
{{/if}}
export class {{{name}}} {
  {{#each properties}}
  {{#if description}}
  /** {{{escapeComment description}}} */
  {{#if isRequired}}
  @ApiProperty({ description: '{{{escapeComment description}}}' })
  {{else}}
  @ApiPropertyOptional({ description: '{{{escapeComment description}}}' })
  {{/if}}
  {{/if}}
  {{>isReadOnly}}{{{name}}}{{>isRequired}}: {{>type parent=../name}}
  {{/each}}
}
{{#if enums}}
{{#unless @root.useUnionTypes}}

export namespace {{{name}}} {

    {{#each enums}}
    {{#if description}}
    /**
     * {{{escapeComment description}}}
     */
    {{/if}}
    export enum {{{name}}} {
        {{#each enum}}
        {{{name}}} = {{{value}}},
        {{/each}}
    }

    {{/each}}

}
{{/unless}}
{{/if}}
